import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.proworks.app',
  appName: 'pro-works',
  webDir: 'dist/pro-works',
  bundledWebRuntime: false
};

export default config;
